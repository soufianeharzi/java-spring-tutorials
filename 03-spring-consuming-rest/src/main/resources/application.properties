spring.application.name=consuming-rest

# Run on port 8081 to avoid conflict with quote-service (which runs on 8080)
server.port=8081

# Quote service configuration
quote.service.base-url=http://localhost:8080
